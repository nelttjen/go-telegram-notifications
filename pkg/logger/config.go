package logger

// log levels
const LoggerLevelNone uint8 = 0
const LoggerLevelImportant uint8 = 1
const LoggerLevelDebug uint8 = 2
const LoggerLevelTrace uint8 = 3

const LogEnabled = 1
const LogDisabled = 0

const LoggerEnabled = LogEnabled
const LoggerLevel = LoggerLevelDebug
